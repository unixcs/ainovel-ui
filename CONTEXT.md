# 小白一号

邀请制多用户的 AI 小说创作控制面（产品名：小白一号）：让受邀操作者在网页上创建作品、启动写作、查看进度并管理成果；底层调用 `ainovel-cli`，但与服务器上已有的 CLI 版运行时完全隔离。

## Language

### 产品与部署

**小白一号**:
本项目的产品名：面向受邀新手的网页创作控制面；不是现网 ainovel-cli 的终端替身，也不是发布系统。
_Avoid_: Ainovel Web（旧草稿名）、网页版 CLI、写作助手

**现网 CLI 版**:
云服务器上已经部署、正在使用的 ainovel-cli 工作区与相关发布配套；小白一号不得占用其目录、容器或作品数据。
_Avoid_: 旧系统、生产 ainovel、/opt/ainovel（路径是实现细节，叙述时用此术语）

**独立栈**:
小白一号自己的 Docker Compose 部署单元，含独立目录、容器、网络与数据卷，与现网 CLI 版隔离。
_Avoid_: 新环境、namespace、compose 项目（实现词）

**本地验证**:
在推送到 GitHub 与云服务器之前，于本地（含 WSL）把独立栈跑通并完成关键路径检查的过程。
_Avoid_: 本地调试、dev 环境（过宽）

**产品仓**:
承载小白一号全部自研代码与部署文件的私有 GitHub 仓库；本地工作根为 `/mnt/demo/ainovel-ui`。
_Avoid_: monorepo（过宽）、模板仓、上游仓

**IP 访问**:
上线初期用服务器公网 IP 与独立入口访问小白一号，用于本地验证之后的云上验收。
_Avoid_: 临时域名、测试链接

**域名接入**:
在 IP 访问验证通过后，将 Cloudflare 域名绑定到小白一号入口；证书与 DNS 凭据不入库。
_Avoid_: 绑域、上 HTTPS（过宽口语）

### 人与账号

**操作者**:
受邀登录本系统、可创建并控制自己作品的人。
_Avoid_: 用户（泛称时保留口语，但领域术语用操作者）、客户、租户主体

**邀请**:
管理员把准入资格发给外部人员的行为与记录；落地形态在 MVP 中是邀请码。
_Avoid_: 公开注册、自行申请

**邀请码**:
管理员生成的、默认一次性且限时的字符串凭证；用于领取操作者账号。管理员可作废。
_Avoid_: 注册码、激活码、邀请链接（V2）

**引导管理员**:
独立栈首次启动时由环境变量创建的首位管理员账号，用于登录后台并发出第一批邀请码。
_Avoid_: root、初始化用户、seed user

**领取账号**:
外部人员提交有效邀请码并设置用户名密码，从而成为操作者的过程。
_Avoid_: 注册、signup、激活

**管理员**:
能发放邀请、停用操作者、查看系统级运行与资源状态的操作者角色。
_Avoid_: 超管、root、站长

### 作品与创作

**作品**:
一本小说及其独立事实目录、运行记录与产物的总称；归属单一操作者。
_Avoid_: 项目、小说文件、书目

**创作指令**:
快速开始（MVP）形成的、用于启动引擎的完整写作意图；未来共创规划也会收敛为同一对象。
_Avoid_: prompt、需求描述、brief

**快速开始**:
用一句话直接生成创作指令并进入创作运行的开写方式；是 MVP 的默认且唯一开写入口。
_Avoid_: 一键开写、简单模式

**共创规划**:
通过多轮澄清逐步整理创作指令的开写方式；本阶段明确延后，不属于 MVP。
_Avoid_: 向导、问卷、chat onboarding


**创作运行**:
某作品上一次可启动、暂停、恢复、终止的引擎执行过程；同一作品同一时刻最多一个活跃运行。
_Avoid_: 任务、job、进程（实现细节）、会话

**活跃创作运行**:
当前正在执行、尚未结束/暂停/熔断的创作运行。平台按操作者与全站两个维度限制其数量。
_Avoid_: 在线任务、running job

**并发上限**:
平台额度的具体数值约束。MVP 固定为：每操作者同时 1 个活跃创作运行，全站同时 2 个。
_Avoid_: 队列长度、worker 数（实现词）

**创作阶段**:
作品在引擎中的宏观阶段，如初始化、设定、大纲、写作、完成。
_Avoid_: status、state（过宽）

**当前流程**:
创作运行中的微观流程，如写作、评审、重写、打磨、处理干预。
_Avoid_: 子状态、workflow step

**干预**:
操作者在创作运行中提交的修改意见；由引擎裁定影响范围后再改变设定、大纲或章节。
_Avoid_: 指令、评论、反馈、chat

**验收许可**:
逐章验收模式下，允许引擎新增下一章的明确许可。
_Avoid_: 通过、approve、continue token

**产物**:
作品落盘结果，包括大纲、角色、世界观、章节正文、摘要、评审与导出文件。
_Avoid_: 输出、output、附件

**只读产物**:
MVP 中供浏览与下载、不允许在网页直接写回引擎事实目录的产物。
_Avoid_: 可编辑稿、在线文档

**后台续写**:
创作运行不依赖浏览器保持打开；操作者离开页面后，活跃运行仍继续直到完成、失败、暂停或熔断。
_Avoid_: 挂机、daemon、后台任务（实现词）

**自动模式**:
创作运行的默认推进方式：引擎在无验收许可的情况下持续写作、评审与返工，直到作品完成、操作者暂停，或触发额度熔断。
_Avoid_: 全自动、无人值守、autonomous

**逐章验收模式**:
非默认的推进方式：每新增一章前必须消耗一次验收许可；返工与结构维护不消耗许可。
_Avoid_: 人工审阅模式、步进模式

**额度熔断**:
因模型凭证侧额度不足、作品预算耗尽或 Provider 明确拒付/限流，导致创作运行主动停止的保护动作。
_Avoid_: 报错停机、欠费、token 用完（口语可保留，术语用额度熔断）

**继续创作**:
操作者在额度熔断、暂停或可恢复失败之后，显式发出的恢复动作；系统不得在额度恢复后自动偷偷续跑。
_Avoid_: 重试、resume job、唤醒

### 模型与隔离

**模型凭证**:
调用 LLM Provider 所需的敏感配置，至少包含服务商标识、API Key、可选 Base URL 与模型名；归属单一操作者，不在操作者之间共享。
_Avoid_: 密钥配置、token 配置、账号（过宽）、全局 Key

**平台额度**:
管理员对服务器资源的上限策略；MVP 先落地并发上限，磁盘为软告警。不是模型充值余额。
_Avoid_: 余额、积分、token 包、充值

**作品隔离**:
不同操作者的作品、模型凭证、创作运行与产物彼此不可见、不可互相读写。
_Avoid_: 租户隔离（实现词）、多租户

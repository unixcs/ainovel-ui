# 活跃创作运行全站克制并发

小白一号对后台创作运行施加硬顶：每位操作者同一时刻最多 1 个活跃创作运行，全站合计最多 2 个。超出时拒绝启动并给出明确忙碌提示，而不是静默排队到资源耗尽。作品数量本身不设硬顶，但“建书”不等于“同时在写”。原因：yun1 已承载其他服务，模型费虽由操作者自担，CPU/内存/磁盘与容器数仍由平台承担；邀请制后台续写下若无硬顶，负载会在无人值守时叠加。磁盘与单作品体积先做软告警，硬顶可后加。

# 创作运行与浏览器解耦；默认全自动，额度耗尽后停止并需人工继续

创作运行必须在操作者关闭网页、断网或更换设备后继续执行，否则 Web 控制面会退化成远程终端，无法满足“小白打开网址就能写小说”的目标。默认自动化模式是全自动推进，而不是逐章验收；当模型额度或作品预算耗尽时，创作运行进入可恢复的停止，而不是静默重试或自动换 Key。额度恢复后，必须由操作者显式点击继续，才会启动新的创作运行或恢复既有 checkpoint。这样成本边界清晰，也避免在欠费/限流状态下把作品写坏。

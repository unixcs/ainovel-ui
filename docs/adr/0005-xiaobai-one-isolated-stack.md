# 「小白一号」独立部署，与现有 ainovel CLI 运行时隔离

Web 产品命名为「小白一号」，是面向受邀新手的图形化创作控制面；它不接管云服务器上已有的 ainovel-cli 工作区、不读写 `/opt/ainovel` 现有作品，也不接入番茄发布流水线。运行时以独立 Docker Compose 栈部署，使用独立目录、独立容器名、独立网络与独立数据卷，避免与现网 CLI 版争用同一作品目录或同一容器。交付顺序是：先在本地汇集上游与自研代码 → WSL 验证 → 推 GitHub → 再一键部署到云服务器。这样现网写作/发布链路保持稳定，新 UI 可以独立迭代与回滚。

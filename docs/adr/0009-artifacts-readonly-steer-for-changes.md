# MVP 产物只读，方向调整走干预

小白一号在 MVP 中把大纲、角色、章节等产物作为只读浏览与下载对象；操作者若要改故事方向，通过干预让引擎裁定并改写，而不是在网页里直接当文档编辑正文。运行中与暂停/完成/熔断后均不提供写回引擎事实目录的在线改稿。原因：后台续写与人工改稿双写同一章会破坏 checkpoint、哈希与续跑一致性；本阶段产品定位是创作控制面，不是在线网文 IDE。若 V2 需要人工改稿，必须先定义与活跃创作运行的互斥规则及“是否仍视为引擎产物”的边界。

# 邀请码 + 账号密码作为进门方式

小白一号不做公开注册。部署时用环境变量引导创建首位管理员；管理员在后台生成邀请码，受邀者凭邀请码设置用户名与密码后成为操作者，此后以密码登录。邀请码默认一次性、限时，管理员可随时作废；只有管理员能发邀请，操作者不能再邀请他人。拒绝 Magic Link 作为 MVP 方案，是因为当前先走 IP 访问、本地 WSL 验证，不宜依赖邮件投递；拒绝纯管理员代建账号，是为了保留“邀请制领取”的产品形态并减少明文传密。后续若加邮件邀请链接，应建立在同一操作者账号模型之上，而不是替换本决策。

# 小白一号 — PRD（Grilling 草案）

> 状态：**Grilling 已收束，MVP 规格冻结；待通知后开工实现。**
> 日期：2026-07-25
> 基础引擎：`voocel/ainovel-cli`

## 决策日志

- 2026-07-25：产品形态定为 **邀请制多用户**（非私人单用户，非公众 SaaS）。
- 2026-07-25：模型费用由 **操作者自带模型凭证** 承担；平台不统一垫付、不做充值计费。
- 2026-07-25：创作运行支持 **后台续写**，与浏览器生命周期解耦。
- 2026-07-25：默认 **自动模式**；触发 **额度熔断** 后停止，恢复额度后需操作者显式 **继续创作**。
- 2026-07-25：MVP 仅提供 **快速开始**；**共创规划** 延后到 V2。
- 2026-07-25：产品名 **小白一号**；与 **现网 CLI 版** 以 **独立栈** 隔离；先 **本地验证**，再 GitHub，再上云。
- 2026-07-25：不接管旧作品，不接入番茄发布；Web MVP 只服务新作品。
- 2026-07-25：交付形态为 **私有产品仓**；本地根目录 `/mnt/demo/ainovel-ui`；云上先 **IP 访问**，再 **域名接入**。
- 2026-07-25：进门方式为 **邀请码 + 账号密码**；引导管理员发码，操作者不能再邀请。
- 2026-07-25：**并发上限** = 每操作者 1 个活跃创作运行，全站 2 个；磁盘先软告警。
- 2026-07-25：产物 **只读**；改方向用 **干预**；不做在线改正文。
- 2026-07-25：Grilling 收束；实现等待用户通知后再开工。

## 0. 当前事实

`ainovel-cli` 已经解决“如何创作小说”的核心引擎问题：

- 一句话快速开始或多轮共创规划
- Architect / Writer / Editor / Arbiter 协作
- 长篇滚动规划、章节写作、评审与返工
- Step 级 checkpoint 与断点恢复
- 写作中实时干预
- 可选逐章验收
- 多 Provider / 多模型配置
- 作品按启动目录隔离，结果写入 `output/novel`
- 支持 Docker headless 运行

因此本项目原则上不重新实现小说引擎，而是补齐“小白可用的 Web 控制面”。

## 1. 第一性原理

### 1.1 用户真正要完成的任务

用户不是来“使用 CLI”的，也不是来“配置多智能体”的。用户真正想完成的是：

> 打开一个网址，描述自己想写的小说，确认费用与控制方式，点击开始；之后即使关掉网页，小说仍能安全继续写，并且随时能看进度、干预、暂停、恢复、验收和下载。

### 1.2 小白产品必须消灭的负担

- 不安装 Go、Docker 或终端工具
- 不理解工作目录、命令行参数、JSON 配置、checkpoint
- 不需要记命令（如 `/review on`、`/next`、`/model`）
- 不直接阅读原始日志来判断系统是否正常
- 不因刷新页面、断网或关闭浏览器丢失任务
- 不会因为重复点击而启动两个写作进程破坏同一本书
- 不会在不清楚成本的情况下无限消耗模型额度
- API Key 不出现在前端日志、页面源码或普通数据库明文中

### 1.3 Web UI 的本质

Web UI 不是“给终端套一个网页壳”，而是一个可靠的小说创作控制面：

```text
浏览器 UI
  -> Web API / 控制面
  -> 作品与运行状态管理
  -> 每本书一个受控 ainovel-cli 运行实例
  -> ainovel-cli 文件系统事实层
  -> 结构化进度、章节、评审、成本、错误
  -> 浏览器实时更新
```

终端输出只适合诊断，不应成为 UI 的主数据源。应优先读取引擎已经落盘的事实文件；启动、暂停、继续、干预等动作由受控进程管理器执行。

## 2. 暂定产品边界

### 2.1 MVP 应有

0. 进门
   - 引导管理员首次登录
   - 管理员生成邀请码
   - 受邀者凭邀请码领取账号并密码登录
1. 首次设置（操作者自己的模型凭证）
   - 选择模型服务商
   - 填自己的 API Key / Base URL / 模型
   - 连接测试
   - 可选单作品预算告警；平台不代付模型费
2. 作品首页
   - 新建作品
   - 作品列表
   - 显示当前阶段、章节进度、最近活动、异常
3. 新建作品
   - 快速开始：一句话形成创作指令（MVP 唯一入口）
   - 可选：风格、目标篇幅、作品预算
   - 共创规划：明确延后，不在 MVP
4. 创作工作台
   - 开始、暂停、恢复
   - 当前阶段与当前 Agent
   - 当前章与已完成章数
   - 可读的活动时间线
   - 实时干预
   - 自动模式 / 逐章验收模式
5. 章节与成果
   - 大纲、角色、世界观、章节列表
   - 阅读章节正文与评审结果
   - 下载单章或整本导出
6. 故障恢复
   - 页面刷新后恢复状态
   - 服务重启后识别已有 checkpoint
   - 防止同一本书并发运行
   - 明确显示“等待用户”“可自动重试”“必须处理”的区别

### 2.2 MVP 暂不默认做

- 重写 ainovel-cli 的 Agent / Engine
- 多人实时协同编辑同一本书
- 小说社区、公开书城、评论、推荐系统
- 在线售卖、订阅、计费平台
- 自动发布到番茄等第三方平台
- 用浏览器终端模拟 TUI 作为正式产品体验
- 完整共创规划多轮对话（延后 V2）
- 将番茄发布流水线并入小白一号 MVP
- 接管现网 CLI 版作品目录或容器

以上边界都等待 grilling 确认。

## 3. 核心用户旅程（假设）

```text
打开网址
  -> 首次配置模型
  -> 新建作品
  -> 快速开始：一句话创作指令
  -> 确认风格/篇幅/可选作品预算与自动模式
  -> 开始创作
  -> 关闭网页（后台续写）
  -> 再次打开查看进度
  -> 必要时干预 / 暂停
  -> 若额度熔断：恢复额度后点继续创作
  -> 阅读和下载成果
```

## 4. 必须明确的领域概念（候选，尚未入 CONTEXT.md）

- **操作者**：打开网页并控制写作的人；是否等于系统账号，待定。
- **作品**：一本小说及其独立事实目录，不只是一个标题。
- **创作指令**：快速输入或共创规划最终收敛出的引擎启动输入。
- **创作运行**：某本作品的一次可启动、暂停、恢复的引擎执行过程。
- **创作阶段**：引擎 Phase（init / premise / outline / writing / complete）的用户化表达。
- **当前流程**：引擎 Flow（writing / reviewing / rewriting / polishing / steering）。
- **干预**：在创作过程中提交、并由引擎判断影响范围的用户意见。
- **验收许可**：逐章验收模式下，允许引擎新增下一章的许可。
- **产物**：大纲、角色、世界观、章节、摘要、评审、导出文件。
- **模型凭证**：调用 LLM Provider 所需的敏感配置。

## 5. 架构分叉点

### 已决策：B. 邀请制多用户系统

- 只有被管理员邀请的操作者可登录
- 每位操作者拥有自己的作品、创作运行与产物
- 作品隔离是硬边界：操作者互不可见
- 不做公开注册、不做公众 SaaS 计费与风控
- 详见 `docs/adr/0001-invite-only-multi-operator.md` 与 `CONTEXT.md`

### 已否决

- A. 私人单用户工具：你明确需要多人使用
- C. 面向公众的 SaaS：会引入计费、风控、滥用防护与合规，超出当前目标

### 已决策：操作者自带模型凭证

- 每位操作者配置并只使用自己的模型凭证
- 平台不提供全局共享 Key，不做充值/余额
- 平台可限制并发创作运行与磁盘，不限制“模型费账户”
- 详见 `docs/adr/0002-operator-owned-model-credentials.md`

### 已决策：后台续写 + 默认自动模式 + 额度熔断

- 关闭网页不停止活跃创作运行
- 默认自动模式，不默认逐章验收
- 额度熔断（模型额度/作品预算耗尽或 Provider 拒付限流）后停止
- 额度恢复后不得自动续跑，必须由操作者点击继续创作
- 逐章验收模式可作为高级选项保留，但不是默认路径
- 详见 `docs/adr/0003-background-run-and-budget-stop.md`

### 已决策：MVP 仅快速开始

- 开写入口只有快速开始
- 共创规划延后到 V2
- 写作中的干预承担“中途改方向”
- 详见 `docs/adr/0004-mvp-quick-start-only.md`

### 已决策：小白一号独立栈

- 产品名：小白一号
- 与现网 CLI 版目录/容器/数据完全隔离
- 不导入旧书，不接入番茄发布
- 交付顺序：本地汇集代码 → WSL 本地验证 → 推 GitHub → 一键部署云服务器
- 详见 `docs/adr/0005-xiaobai-one-isolated-stack.md`

### 已决策：私有产品仓与访问顺序

- 单一私有 GitHub 产品仓，不做公开自托管模板
- 本地工作根：`/mnt/demo/ainovel-ui`
- 云上独立目录（如 `/opt/xiaobai-one`），与 `/opt/ainovel` 并列隔离
- 先 IP 访问验收，再绑定 Cloudflare 域名；CF Token 不入库
- 上游引擎用官方镜像，不 vendor 现网数据
- 详见 `docs/adr/0006-private-repo-ip-then-cf.md`

### 已决策：邀请码进门

- 环境变量创建引导管理员
- 管理员生成一次性、限时、可作废的邀请码
- 受邀者凭码设置用户名密码（领取账号）
- 操作者不能邀请其他人
- 不做公开注册，MVP 不做邮件 Magic Link
- 详见 `docs/adr/0007-invite-code-password-auth.md`

### 已决策：并发上限

- 每操作者同时最多 1 个活跃创作运行
- 全站同时最多 2 个活跃创作运行
- 超限时明确拒绝并提示，不静默拖垮主机
- 作品数量不硬顶；磁盘先软告警
- 详见 `docs/adr/0008-run-concurrency-caps.md`

### 已决策：产物只读

- 大纲/角色/章节仅浏览与下载
- 改故事方向走干预，不提供网页写回正文
- 在线改稿若需要，留给 V2 并先定义与活跃运行的互斥
- 详见 `docs/adr/0009-artifacts-readonly-steer-for-changes.md`

## 6. 初步技术方向（未决策）

- 尽量上游兼容：Web 层作为独立适配器，不侵入 ainovel-cli 核心。
- 每本作品使用独立目录；同一作品最多一个写进程。
- 后台任务与浏览器生命周期分离。
- 进度以落盘事实为准，日志为辅。
- 实时更新优先使用 SSE；只有确需双向持续通信时才使用 WebSocket。
- 进程可先用本机 Docker 管理；达到规模后再考虑队列和多节点 Worker。
- 前端绝不持久保存明文 API Key；后端应做受限存储与脱敏展示。
- 公开公网访问前必须有身份认证、HTTPS、CSRF/限流和备份策略。

## 7. Grilling 决策队列

1. ~~这是单用户私人工具、邀请制多用户，还是公众 SaaS？~~ → **已选 B：邀请制多用户**
2. ~~“网页直接访问”具体是部署在 `yun1` 的固定网址，还是 GitHub Codespaces / 一键部署？~~ → **私有仓 + 云上独立栈；先 IP，后 CF 域名**
3. ~~API Key 由服务器主人统一提供，还是每个用户自带？~~ → **已选 A：操作者自带模型凭证**
4. ~~关掉网页后是否必须继续自动写？~~ → **已选 A：必须后台续写**
5. ~~默认是全自动写完整本，还是默认逐章确认？~~ → **默认自动模式；写到额度熔断停止，之后手动继续创作**
6. ~~用户最需要控制的是题材与风格，还是细粒度大纲、角色和每章内容？~~ → **开写前一句话+风格/篇幅；中途用干预；正文只读**
7. ~~MVP 是否必须包含“共创规划”完整多轮体验？~~ → **否；仅快速开始**
8. ~~是否复用现有 `/opt/ainovel` 作品，还是新 UI 只创建新作品？~~ → **只创建新作品；不接管现网 CLI 版**
9. ~~是否把现有番茄发布流水线纳入同一界面？~~ → **否；发布保持独立**
10. ~~可接受的模型成本、并发数和服务器资源上限是多少？~~ → **模型费自担；并发 1/人、2/全站；磁盘软告警**
11. ~~管理员如何发出第一份邀请？邮箱、一次性链接，还是仅生成邀请码？~~ → **邀请码；引导管理员环境变量创建**
12. ~~一位操作者默认同时允许几个活跃创作运行？~~ → **1；全站最多 2**

## 8. 当前验收定义（最小草案）

一个完全不懂命令行的用户能够：

1. 凭邀请码领取账号并登录；
2. 只通过浏览器完成自己的模型凭证配置；
3. 用一句中文描述创建一本新小说；
4. 点击一次开始；
5. 关闭并重新打开网页后仍能看到同一本作品和真实进度；
6. 对运行中的小说提交一次干预；
7. 暂停并从 checkpoint 恢复；
8. 在额度熔断停止后，恢复额度并显式继续创作；
9. 阅读至少一章并下载成果；
10. 全程不接触 SSH、Docker、JSON 或终端命令。

## 9. 交付与隔离约束

1. 小白一号是独立产品，不是现网 CLI 版的皮肤。
2. 云上使用独立 Docker Compose 栈与独立数据目录，禁止挂载或改写现网 CLI 版作品路径。
3. 开发顺序固定为：
   - 本地目录汇集上游 `ainovel-cli` 所需能力与自研 Web 控制面
   - WSL 本地验证
   - 推送到 GitHub
   - 再一键部署到云服务器
4. 现网 CLI 版与发布流水线继续独立运行；出问题互不影响是硬要求。

## 10. 冻结的 MVP 规格

### 10.1 一句话

受邀操作者打开小白一号 → 领取账号 → 填自己的模型凭证 → 一句话快速开始 → 后台自动写 → 关网页也继续 → 额度熔断后手动继续 → 阅读/下载产物；全程与现网 CLI 版隔离。

### 10.2 角色与权限

| 角色 | 能做 |
|---|---|
| 引导管理员/管理员 | 登录、发/作废邀请码、停用操作者、看全站活跃运行与资源概览 |
| 操作者 | 领账号、配自己的模型凭证、建作品、开始/暂停/继续/干预、读产物、下载 |
| 未邀请访客 | 只能看到登录/领取账号页 |

### 10.3 页面清单

1. 登录页
2. 领取账号页（邀请码 + 用户名密码）
3. 管理员：邀请码管理
4. 管理员：运行与资源概览（简单）
5. 操作者：模型凭证设置（连接测试、脱敏展示）
6. 作品列表
7. 新建作品（快速开始表单）
8. 创作工作台（阶段/流程/进度/时间线/干预/暂停/继续）
9. 产物浏览（大纲/角色/章节阅读）
10. 下载页或下载动作

### 10.4 创作运行状态（用户可见）

```text
未开始
  -> 启动中
  -> 运行中（后台续写）
  -> 已暂停
  -> 额度熔断
  -> 失败（需处理）
  -> 已完成
```

规则：

- 同一作品同一时刻至多一个活跃创作运行
- 关闭浏览器不改变“运行中”
- 额度熔断与暂停都不是完成
- 额度熔断后只有显式“继续创作”才能再跑
- 全站/个人并发上限在“启动中”前检查

### 10.5 独立栈（逻辑拓扑）

```text
[浏览器]
   |
   v
[xiaobai-web] -----> [xiaobai-api]
                         |
                         +--> 账号/邀请/作品元数据 DB
                         +--> 操作者模型凭证（加密存储）
                         +--> 作品数据目录（按操作者隔离）
                         +--> 创作运行管理器
                                |
                                v
                         [ainovel-cli 容器]  (官方镜像，按运行即建)
```

约束：

- Compose 项目名/容器名/数据目录均带 `xiaobai` 前缀
- 云上数据根示例：`/opt/xiaobai-one`
- 禁止挂载 `/opt/ainovel` 或现网发布目录
- 先暴露独立端口做 IP 访问；域名接入后由反代转发，不抢占现网 443 业务除非显式配置

### 10.6 本地与交付

```text
/mnt/demo/ainovel-ui          # 产品仓工作根
  apps/web
  apps/api
  deploy/                     # compose、.env.example、一键脚本
  docs/
  CONTEXT.md
  README.md
```

顺序：本地实现 → WSL `docker compose up` 验证 → 推私有 GitHub → 云上 `/opt/xiaobai-one` 一键部署 → IP 验收 → 可选 CF 域名接入。

### 10.7 明确不做（MVP）

- 公开注册与 SaaS 计费
- 平台统一垫付模型费
- 共创规划多轮对话
- 接管现网 CLI 版作品
- 番茄发布集成
- 浏览器内嵌 TUI
- 操作者互相可见/协作同一作品
- 全站无限并发
- 网页内直接编辑章节正文并写回引擎目录

## 11. 实现待命

Grilling 已结束。在用户明确通知开工之前：

- 不创建应用脚手架
- 不写业务代码
- 不部署云上独立栈
- 不申请/写入 Cloudflare Token

开工后的建议第一刀：

1. 在 `/mnt/demo/ainovel-ui` 建 `apps/web`、`apps/api`、`deploy/` 骨架
2. 落地账号/邀请码/会话
3. 落地模型凭证加密存储与连接测试
4. 落地作品目录与创作运行管理器（含并发上限、后台续写、额度熔断）
5. 接官方 `ainovel-cli` 镜像做一次 WSL 端到端快速开始
6. 再推私有 GitHub，最后云上 `/opt/xiaobai-one` IP 验收
